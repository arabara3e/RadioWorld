te propose une revue de code prête à copier-coller, classe

GetEERDataManagementImpl.java

Points positifs

Découpage clair load → map → return.

Tests unitaires présents.

Utilisation d’un DTO de sortie dédié.


Améliorations proposées

Nommage : mainProcedure → loadFolder, aImResponse → buildResponse (et corriger le « I » majuscule).

Validation d’entrée : rejeter null/blank pour clientId (ex: Assert.hasText(clientId, "clientId must not be empty"); ou @Pattern côté API).

Repo : faire retourner un Optional<Folder> (selectFolderByKey) et utiliser ifPresent.

Gestion d’erreurs : capturer DataAccessException et relancer une exception métier (EerCorporateException) avec code/message standardisés.

Logging : messages plus informatifs et structurés, sans PII.
Exemple: LOG.info("RetrieveEERData request clientId={}", safeClientId(clientId));

Mapper dédié : extraire la construction de réponse dans un Folder→CustomerEERDataGetResponseMapper (facilement testable).

Horloge : éviter now() disséminé; si nécessaire, injecter un Clock.


Mini-extrait

public CustomerEERDataGetResponse getByClientId(String clientId) {
  Assert.hasText(clientId, "clientId must not be empty");
  LOG.info("Retrieve EER data clientId={}", clientId);

  var folderOpt = folderRepository.selectFolderByKey(clientId); // Optional<Folder>
  return folderOpt.map(folderMapper::toResponse)
                  .orElseGet(CustomerEERDataGetResponse::notFound);
}


---

GetEERDataWorking.java

Points positifs

Objet « tampon » simple, code lisible.


Améliorations proposées

L’objet de travail est mutables + état dispersé : on peut soit

le supprimer au profit de variables locales, soit

le rendre immuable (record) si tu veux garder la structure.


Éviter now() dans ce type d’objet; si l’heure est utile, passer une LocalDateTime depuis le service (ou Clock).

Harmoniser les noms et commentaires en anglais pour cohérence d’équipe.


Alternative concise

public record GetEERDataWorking(
    String clientId,
    Folder folder,
    LocalDateTime processingTime,
    CustomerEERDataGetResponse response
) {}


---

CustomerEERDataGetResponse.java

Points positifs

DTO séparé du domaine, clair.

Champs métiers explicites (priority, requestedDate, …).


Améliorations proposées

Immutabilité : passer en builder/record (ou Lombok @Value @Builder) pour éviter setters.

Noms homogènes : crcUserId vs cacmUserId → s’assurer de la cohérence (acronymes validés métier).

Dates : si possible LocalDate avec une configuration Jackson (@JsonFormat(pattern="yyyy-MM-dd")) plutôt que String.

Méthode utilitaire : ajouter un static notFound() pour le cas 204/empty.


Mini-extrait

@Value
@Builder
public class CustomerEERDataGetResponse {
  boolean folderFound;
  String crcUserId;
  String cacmUserId;
  String priorityCode;
  LocalDate requestedDate;
  String priorityReason;

  public static CustomerEERDataGetResponse notFound() {
    return CustomerEERDataGetResponse.builder().folderFound(false).build();
  }
}


---

ApiRetrieveEERData.java (controller)

Points positifs

Annotations OpenAPI complètes 👍

Méthode returnError centralisée.


Améliorations proposées

Validation : @Pattern ou @Size sur @PathVariable("clientId") + @Validated (déjà présent) → garantir le 400.

Statuts :

Si folderFound=false → envisager 204 No Content (tes tests semblent déjà le viser).

Sinon 200 OK avec DTO.


Transactionnel : @Transactional(readOnly = true) si tu veux, mais non indispensable sur un contrôleur si la couche service l’a déjà.

Logs : log d’entrée + durée (optionnel, via StopWatch).


Mini-extrait

@GetMapping(value="/v1/clients/{clientId}/directories", produces = MediaType.APPLICATION_JSON_VALUE)
public ResponseEntity<?> retrieve(@PathVariable @Pattern(regexp="\\d{10,}") String clientId,
                                  @RequestHeader("UserId") String userId) {
  var resp = getEERDataManagement.getByClientId(clientId);
  if (!resp.isFolderFound()) return ResponseEntity.noContent().build();
  return ResponseEntity.ok(customerEERDataDTOMapper.map(resp));
}


---

CustomerEERDataDTO.java

Points positifs

Builder manuel lisible.

DTO d’exposition distinct du DTO domaine.


Améliorations proposées

Lombok pour alléger le builder ou passer par MapStruct pour la conversion.

Null-safety : s’assurer que les champs trimés le sont sans NPE (voir mapper).

@Schema au niveau champ si l’OpenAPI doit afficher des exemples (example = "HIGH" / "2025-01-31").



---

CustomerEERDataDTOMapper.java

Points positifs

Mapper séparé ✅


Améliorations proposées

Les getXxx().trim() peuvent lever un NPE si null.
→ Utiliser StringUtils.trimToNull() (Apache) ou utilitaire local.

Isoler la logique de formatage de dates dans le mapper (unique point).


Mini-extrait

public static CustomerEERDataDTO map(CustomerEERDataGetResponse r) {
  return CustomerEERDataDTO.builder()
      .setCrcUserId(trim(r.getCrcUserId()))
      .setCacmUserId(trim(r.getCacmUserId()))
      .setPriorityCode(trim(r.getPriorityCode()))
      .setRequestedDate(r.getRequestedDate() != null ? r.getRequestedDate().toString() : null)
      .setPriorityReason(trim(r.getPriorityReason()))
      .build();
}
private static String trim(String s){ return s==null? null : s.trim(); }


---

GetEERDataManagementImplTest.java

Points positifs

Cas « success » et « empty folder » couverts.

Usage de Mockito clair.


Améliorations proposées

Utiliser @ExtendWith(MockitoExtension.class) au lieu de initMocks.

Ajouter les cas :

clientId vide → IllegalArgumentException (ou 400 via contrôleur).

Exception repo (DataAccessException) → EerCorporateException.

Vérifier le format de date attendu.


Éviter les magic strings : extraire en constantes (CLIENT_ID_OK, REQUESTED_DATE).

Retirer andDo(print()) des tests d’API (bruite les logs de CI).



---

ApiRetrieveEERDataTest.java

Points positifs

Tests 200/400/500 bien posés.

Mock MVC bien utilisé.


Améliorations proposées

Ajouter le cas 204 No Content (folder not found).

Vérifier le content-type + payload pour 200 via jsonPath.

Regrouper l’URL en constante (URL_GET_EER_DATA).

Test du header UserId manquant (si requis) → 400.


Mini-extrait

mockMvc.perform(get(URL_GET_EER_DATA, clientId).header("UserId","User123"))
  .andExpect(status().isOk())
  .andExpect(content().contentType(MediaType.APPLICATION_JSON))
  .andExpect(jsonPath("$.priorityCode").value("PR"));


---

BaseResourceTest.java

Remarque rapide

Si ce test ne vérifie que la présence de beans ou l’initialisation de contexte, ok.
Évite de lister des impls concrètes si le wiring peut changer; préfère un @SpringBootTest smoke test minimal.



---

Petites recommandations transverses

Uniformiser la langue dans le code (anglais) et garder le français pour la doc fonctionnelle.

Pas de PII dans les logs (masquer/anonymiser les identifiants si sensibles).

Constantes : extraire codes erreurs, patterns de validation, formats de date.

Sonar/Qualité : activer les règles OptionalUsedAsField, NullAway/SpotBugs si possible.

MapStruct : si la cartographie se multiplie, ça simplifiera (et évite les trim() oubliés).



---

Si tu veux, je peux te proposer un patch consolidé (service + mapper + validations + 4 tests complémentaires) sur la base de vos conventions.

